---
name: Switch
menu: Components
route: /components/switch
---

import { Component } from 'react';
import Switch from 'test-helpers/controlleds/switch';
import { Switch as SimpleSwitch, Label } from '../../..';
import { Playground, PropsTable } from 'docz';
import './switch.scss';

# Switch Component

This is the Switch component, to deal with native components like `checkbox` and `radio`.

## Playground

<Playground>
  <Switch checked type="checkbox" id="on" name="on" value="test">
    <Label htmlFor="on">On</Label>
  </Switch>

  <Switch type="checkbox" id="off" name="off" label="Off" value="test">
    <Label htmlFor="off">Off</Label>
  </Switch>

  <Switch checked disabled type="checkbox" id="on-disabled" name="on-disabled" value="test">
    <Label htmlFor="on-disabled" disabled>On (Disabled)</Label>
  </Switch>

  <Switch disabled type="checkbox" id="off-disabled" name="off-disabled" value="test">
    <Label htmlFor="off-disabled" disabled>Off (Disabled)</Label>
  </Switch>
</Playground>

## Properties Table

<PropsTable of={SimpleSwitch} />
