---
name: Button
menu: Components
route: /components/button
---

import { Button } from '../../lib';
import { Playground, PropsTable } from 'docz';
import '../main.scss';

# Button Component

This is the button component.

## Types and sizes

<Playground className="row colapse">
  <div className="column vertical-spacing">
    <Button primary>Primary</Button>
    <Button primary medium>Primary Medium</Button>
    <Button primary large>Primary Large</Button>
  </div>

  <div className="column vertical-spacing">
    <Button secondary>Secondary</Button>
    <Button secondary medium>Secondary Medium</Button>
    <Button secondary large>Secondary Large</Button>
  </div>

  <div className="column vertical-spacing">
    <Button tertiary>Tertiary</Button>
    <Button tertiary medium>Tertiary Medium</Button>
    <Button tertiary large>Tertiary Large</Button>
  </div>
</Playground>

## State (enabled and disabled)

<Playground className="row collapse">
  <div className="column vertical-spacing">
    <h3>Primary</h3>
    <Button primary medium>Enabled</Button>
    <Button primary medium disabled>Disabled</Button>
  </div>

  <div className="column vertical-spacing">
    <h3>Secondary</h3>
    <Button secondary medium>Enabled</Button>
    <Button secondary medium disabled>Disabled</Button>
  </div>

  <div className="column vertical-spacing">
    <h3>Tertiary</h3>
    <Button tertiary medium>Enabled</Button>
    <Button tertiary medium disabled>Disabled</Button>
  </div>
</Playground>

## Box types

<Playground className="vertical-spacing">
  <Button primary>Default</Button>
  <Button primary block>Block</Button>
</Playground>

## Properties Table

<PropsTable of={Button} />

## About button `type`

You can set with the values `button`, `submit` or `reset`, same as `<button type="">` HTML tag attr.
